
%header
 
  %h1 Tumblr Follower Counts

%main

  %section.followers
    %header
      .widgets
        - @blogs.each do |blog|
          .blog
            %iframe{ src: "followers/#{blog["name"]}" }
            %button{ onclick: "location.href='panicboard://?url=#{CGI.escape(url("/followers/#{blog["name"]}?token=#{current_user.oauth_token}"))}&panel=diy'" }
              Add to Status Board
